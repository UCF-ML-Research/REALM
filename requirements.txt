# REALM — Python dependencies
# Tested with Python 3.10+ and CUDA-enabled PyTorch.
# Install with:  pip install -r requirements.txt
# A few components (PhysPatch's Semantic-SAM, OpenAI CLIP) need the
# git-based installs listed at the bottom of this file.

# ── Core deep learning ────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.36.0,<5.0
open_clip_torch>=2.20.0
einops
kornia
torch-dct
kmeans-pytorch

# ── Diffusion / latent-diffusion (AdvDiffVLM: vendored ldm + taming) ───
pytorch-lightning
omegaconf
ftfy
regex

# ── Vision & segmentation (PAD, PhysPatch) ────────────────────────────
opencv-python
scikit-image
scikit-learn
scipy
pycocotools
albumentations

# ── Model APIs ────────────────────────────────────────────────────────
qwen-vl-utils>=0.0.2
openai>=1.3.0
requests
tenacity

# ── Data handling & utilities ─────────────────────────────────────────
numpy
Pillow>=9.0.0
matplotlib
tqdm
natsort
more-itertools
packaging
PyYAML

# ── Special installs (not plain PyPI wheels) ──────────────────────────
# OpenAI CLIP (advedm / advedm_r / coa surrogate extractors):
#   pip install git+https://github.com/openai/CLIP.git
#
# Segment Anything (PAD defense; PhysPatch masks):
#   pip install git+https://github.com/facebookresearch/segment-anything.git
#
# Detectron2 (PhysPatch's Set-of-Mark / Semantic-SAM) — needs the wheel pkg
# and build isolation disabled so it sees the installed torch:
#   pip install wheel
#   pip install --no-build-isolation git+https://github.com/facebookresearch/detectron2.git
#
# vLLM — ONLY for the eval/serving stage (evaluate_adversarial.py, figstep/
# promptinject injection-text). Driver-sensitive; install a build matching
# your CUDA driver, e.g.:  pip install vllm   (or a cuXXX-matched version)
